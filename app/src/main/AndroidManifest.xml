<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ne.yield_predictor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".getresult"></activity>
        <activity android:name=".resultpage" />
        <activity android:name=".upload_activity" />
        <activity android:name=".LoginActivity"/>
        <activity android:name=".HMBS"/>
        <activity android:name=".HMRS"/>
        <activity android:name=".MBS"/>
        <activity android:name=".MRS"/>
        <activity android:name=".selectitype"/>
        <activity android:name=".OtpActivity"/>
        <activity android:name=".Upload"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>